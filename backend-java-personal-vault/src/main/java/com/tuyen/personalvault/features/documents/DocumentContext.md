# Documents Feature

Manages private document metadata and file storage, always scoped to the authenticated user. See `01-share-docs/API_SPEC.md` (Documents) and `01-share-docs/DATABASE.md` (`documents`).
